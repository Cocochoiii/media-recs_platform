# ============================================
# Media Recommender System - Minimal Dependencies
# Compatible with Python 3.9 - 3.14
# ============================================

# Core ML/DL Framework (required)
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0

# NLP / Transformers (required)
transformers>=4.30.0
tokenizers>=0.13.0

# API Framework (required)
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Testing (required)
pytest>=7.4.0

# Utilities (required)
tqdm>=4.65.0
pyyaml>=6.0.0
requests>=2.28.0

# ============================================
# OPTIONAL - Install manually if needed:
# ============================================
# pip install gensim           # Word2Vec (may not work on Python 3.14)
# pip install spacy            # NLP processing
# pip install sentence-transformers
# pip install redis            # Caching
# pip install sqlalchemy asyncpg psycopg2-binary  # Database
# pip install celery           # Distributed tasks
# pip install mlflow           # Experiment tracking
